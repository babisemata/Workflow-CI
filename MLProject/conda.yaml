name: mlflow_env
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - mlflow
      - pandas
      - scikit-learn
      - joblib
      - matplotlib
      - seaborn
      - scipy
